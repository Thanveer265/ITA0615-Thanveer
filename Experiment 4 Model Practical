import numpy as np
from sklearn.mixture import GaussianMixture
X = np.array([[1.0], [1.5], [3.0], [5.0], [6.0], [7.0]])
gmm = GaussianMixture(n_components=2, random_state=0)
gmm.fit(X)
labels = gmm.predict(X)
print("Cluster Labels:", labels)
print("Means:", gmm.means_)
